export enum LoggerLevel {
  /**
   * Disable all logs
   */
  Off = 'Off',

  /**
   * Only main server infomation
   */
  Main = 'Main',

  /**
   * All logs with requests debug
   */
  Debug = 'Debug',
}
